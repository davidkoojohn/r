<%= link_to 'Creat Article', new_article_path %>

<% @articles.each do |article| %>
    <h1>
      <%= link_to article.title, article_path(article) %>
      <%= link_to 'edit', edit_article_path(article) %>
      <%= link_to 'Delete', article_path(article), method: :delete %>
    </h1>
<% end %>